[pytest]
# Añadimos src/ al PYTHONPATH para que `from edgar_insider.parse...` resuelva
# sin tener que instalar el paquete (pip install -e .). Cuando el proyecto
# crezca y movamos a pyproject.toml, esto se migrará allí.
pythonpath = src
testpaths = tests
